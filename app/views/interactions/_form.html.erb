<%= form_for(@interaction) do |f| %>
  <% if @interaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interaction.errors.count, "error") %> prohibited this interaction from being saved:</h2>

      <ul>
      <% @interaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
    <%= f.label :application_id, :class => "active" %>
    <%= f.number_field :application_id %>
  </div>
  <div class="input-field">
    <%= f.label :title, :class => "active" %>
    <%= f.text_field :title %>
  </div>
  <div class="input-field">
    <%= f.label :type, :class => "active" %>
    <%= f.number_field :type %>
  </div>
  <div class="input-field">
    <%= f.label :date, :class => "active" %>
    <%= f.date_select :date %>
  </div>
  <div class="input-field">
    <%= f.label :contact_id, :class => "active" %>
    <%= f.number_field :contact_id %>
  </div>
  <div class="input-field">
    <%= f.label :details, :class => "active" %>
    <%= f.text_area :details %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
